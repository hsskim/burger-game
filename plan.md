# plan.md — Burger Game v3 확장 계획

2026-07 기준 확장 로드맵. 기획 원본은 [prd.md](prd.md), 개발 가이드는 [CLAUDE.md](CLAUDE.md) 참조.
각 단계는 기존 원칙(단일 파일, 의존성 제로, 데이터 주도)을 유지하면서 진행하고, 완료 시 실제 플레이 가능한 상태로 마무리한다.

**v2 완료**: 스테이지 10개 확장, 무한 모드, UI 디자인 시스템 리팩터, 모바일 표시 대응까지 반영·배포됨.
**v3 목표**는 다국어 지원과, v2에서 미완으로 남은 배포·수익화다.

## 목표

1. 스테이지를 5개에서 10개로 확장 — ✅ v2 완료
2. 스토리(스테이지) 모드와 별개로, 절차 생성 랜덤 맵으로 계속 플레이하는 **무한 모드** 추가 — ✅ v2 완료
3. **다국어 지원**: 한국어/English 전환 (v3 신규)
4. 커스텀 도메인으로 배포하고 Google AdSense 광고 부착

## 확정된 결정사항

- **랜덤 맵은 순수 절차 생성**: 템플릿 풀 방식이 아니라 미로 생성 알고리즘 + 검증 파이프라인으로 만든다.
- **모바일 플레이는 지원하지 않는다** (터치 컨트롤 없음). 다만 모바일 접속 시 페이지가 깨져 보이면 안 되므로 "표시 방침"은 아래에 정의한다.
- **도메인 구입 (잠정)**: 커스텀 도메인은 브랜딩·장기 운영 측면에서 사용한다. AdSense의 핵심 소유권 조건은 사이트 HTML에 검증 코드를 넣을 수 있는 접근 권한이므로, `github.io`가 기술적으로 절대 불가하다고 단정하지 않는다. 호스팅은 GitHub Pages 유지 + 커스텀 도메인 연결을 우선한다.
- **단일 `index.html` 유지**. 예외는 두 가지뿐:
  - 웹폰트 CDN (기존 예외)
  - AdSense 관련: 광고 스크립트(배포 도메인에서만 로드), `privacy.html` 별도 페이지
- **다국어 번역 범위는 전체 완전 번역**: 캔버스 버튼·HUD·팝업·온보딩과 스테이지 이름(10개)·실패 랜덤 코멘트뿐 아니라, 모바일 안내 DOM, `<html lang>`, `<title>`, meta description까지 한/영 두 벌을 준비한다.
- **기본 언어는 브라우저 자동 감지** (`navigator.language`). 사용자가 직접 전환하면 그 선택을 `localStorage`에 저장해 다음 방문에도 유지한다.

## 모바일 표시 방침 (플레이 미지원)

- 캔버스는 CSS로 뷰포트에 맞춰 **축소 표시**한다 (960×640 비율 유지, `max-width: 100vw` 스케일링). 가로 스크롤·깨진 레이아웃이 생기지 않게 한다.
- 터치 전용 기기(`pointer: coarse` 미디어 쿼리 또는 touch 이벤트 감지)에서는 캔버스 위에 **"이 게임은 PC 키보드 전용입니다 🍔" 안내 오버레이**를 표시한다.
- 이렇게 하면 모바일 방문자에게 최소한의 정상적인 첫인상을 주고, AdSense 심사 시 모바일에서 깨진 페이지로 평가되는 것을 피한다.

---

## 진행 상황

**v2 (완료·배포됨)**
- [x] 단계 1 — 공통 기반 리팩터 (동적 셀 크기 · BFS 솔버 · 모드 추상화)
- [x] 단계 2 — 스테이지 6~10 추가 (선택 화면 5×2 · 신규 5스테이지, 솔버 검증 완료)
- [x] 단계 3 — 무한 모드 (절차 생성)
- [x] UI 디자인 시스템 고도화 및 리팩토링 (디자인 토큰 도입, 명시적 팝업 크기 관리, 무한 모드 결과 화면 개선)
- [x] 모바일 표시 대응 (viewport·반응형 캔버스·터치 안내 오버레이), README v2 갱신

**v3 (진행 예정)**
- [ ] 단계 4 — 다국어 지원 (한국어/English)
- [ ] 단계 5 — 배포·수익화

무한 모드 구현 메모:
- 타이틀에 "무한 모드(Space)" 버튼 추가. 라운드마다 `generateStage(round)`로 검증 통과 맵 생성.
- 시드 대신 생성된 맵 전체를 `currentStageData`에 저장 → R 재시작 = 같은 맵(재생성 불필요).
  이 선회로 3-1의 "시드 RNG로 통일"은 폐기됐고, 함정 이모지는 `parseStage`의 `Math.random()`으로 남았다.
  **결정(2026-09): 현행 유지.** 재도전할 때 쓰레기 그림이 바뀌는 편이 덜 지루하다. 기능 영향은 없다
  (함정은 종류와 무관하게 동일 동작). 고정하려면 맵 데이터에 함정 종류를 담아야 해 prd.md의 문자 매핑까지
  바뀌므로, 그만한 이득이 없다.
- 난이도 곡선은 `ENDLESS` 상수(맵 크기 9→11→13, 재료 2→8, 함정·시간여유). 제한시간 = 최적시간 × margin.
- 되돌아가기 최소화: ① 막다른 골목(이웃 1칸) 셀엔 재료를 두지 않음, ② 여러 후보를 생성해
  재방문 칸(revisits)이 가장 적은 맵을 채택(맵 크기별 시도 상한 150/90/50 — 큰 맵 전환 끊김 방지).
  효과: 첫 유효 맵 대비 revisits 대폭 감소(예: 라운드5 10.8→0, 라운드9 22.6→1.9).
- 목숨(하트) 시스템: 런 시작 3개, 실패마다 −1(같은 맵 재도전, 하트 깨짐 연출 후 실패 팝업), 0이면 런 종료.
  실패로 잃은 뒤 3별(완벽) 클리어 시 +1 회복(상한 3). 좌상단 HUD 하트. `ENDLESS.startLives/maxLives`로 조정.
  무한 모드에선 플레이 중 자유 재시작(R)·클리어 후 재시작을 막아 목숨 우회/파밍 방지.
  완전 실패 시(목숨 0) '새 게임' 옵션을 제거하고 '타이틀로 나가기'만 노출하여 런을 확실히 종료.
  (2026-09) 이 화면에만 안내 없이 남아 있던 R=새 런 키 처리를 제거해 위 결정과 코드를 일치시켰다.
  이제 무한 모드에서 R은 어느 상황에서도 동작하지 않는다.
- 온보딩 팝업: **두 모드 모두 첫 진입 때 1회만 표시**한다(2026-09 결정·구현 완료). 무한 모드가 새 런마다
  띄우던 동작은 README·`ONBOARD_ENDLESS_KEY` 상수와 어긋나 있어 스토리 모드와 같은 규칙으로 통일했다.
  게임 아이콘으로 규칙 안내.
  본 기록은 `burger-game-onboard-story/-endless` 키에 저장. 팝업 크기는 상태별로 명시적 하드코딩.
- 최고 기록은 `burger-game-endless` 키에 `{bestRound, bestAvg}`로 저장(별점 저장과 분리). 최고 라운드 +
  그 런의 평균 별점을 함께 기록(더 멀리 갔거나 같은 라운드에서 더 깔끔하면 갱신). 게임오버 화면엔 이번 런
  요약(도달 라운드·평균 별점), 타이틀엔 최고 기록(라운드·평균 별점) 표시.
- 맵 크기 구간: 1~5=9×9 / 6~14=11×11 / 15+=13×13.
- 제한시간: 맵 크기 구간별 고정(1~5=10초 / 6~14=15초 / 15+=20초). 솔버로 확인 결과 전 라운드에서
  최적 대비 최소 1.5배 이상 여유라 항상 공정. `ENDLESS.time(r)` 한 곳에서 조정.
- 난이도 곡선은 15라운드에서 의도적으로 평탄화(재료 종류가 8개뿐이라 자연 상한). 15+는 최대 난이도를
  목숨 3개로 얼마나 오래 버티는지 겨루는 지구력전. 별도 점수 시스템은 두지 않고 "도달 라운드 + 평균 별점"이
  기록 지표. (2026-07 결정)
- 검증: 라운드 1~25 생성 전부 solver 통과(실패 0). 재도전 상태 전이 확인.
- 생성 시간 실측(2026-09, 라운드별 5회): 라운드1 0.8ms / 5 9.4ms / 6 19.8ms / **10 74ms / 14 90ms(최대 98ms)**
  / 15 68.8ms / 30 72.8ms (중앙값). **최악 구간은 13×13이 아니라 11×11의 라운드 10~14**로, 13×13은 시도
  상한이 50으로 낮아 오히려 더 빠르다. 동기 호출이라 그만큼 프레임이 멈춘다(최대 약 6프레임 드롭).
  **결정(2026-09): 현행 유지.** 시도 상한을 낮추면 생성은 빨라지지만 revisits가 늘어 되돌아가기 많은 맵이
  나온다. 맵 품질이 우선이고 끊김은 라운드 전환 순간의 일회성이라 감수한다. 이 항목은 재론하지 않는다.

솔버 강화: 게임은 밟는 즉시 먹으므로, 별 3개(순서 정확) 경로 검사는 함정뿐 아니라 "아직 먹을 차례가
아닌 미래 재료 칸"도 벽으로 막고 순서대로 도달 가능한지 확인한다(`perfectPath`).

6~10 절차 생성(재귀 백트래킹 미로 + 루프) 규칙:
- 재료를 서로 최소 거리 이상으로 흩뿌리고, S에서 최근접 순회 순서로 레시피를 배정해 되돌아가기를 최소화.
- 함정은 "함정 무시 최단 경로"(지름길) 위에 얹어, 안전 경로가 우회하도록 = 능동적 함정 회피.
- 맵 크기: 6~9는 11×11, 10만 13×13.
- 제한 시간: 1~10 전부 15초 (전 스테이지 solver 최적 시간 ≤ 9.5s로 timeOk 충족).

## 단계 1 — 공통 기반 리팩터

이후 모든 단계가 의존하는 기반 작업. 기능 변화는 없고 기존 5스테이지가 그대로 동작해야 한다.

### 1-1. 동적 셀 크기

- `CELL = 48` 고정 상수를 제거하고, 스테이지 로드 시 `cellSize = min(48, floor(GAME_SIZE / max(rows, cols)))`로 계산해 스테이지 상태에 보관.
- 캐릭터 크기(현재 46px 고정)와 셀 이모지 폰트(30px)도 셀 크기에 비례하도록 변경.
- 효과: 9×9 제한이 풀려 11×11, 13×13 맵 사용 가능 (단계 2·3의 난이도 축).

### 1-2. BFS 솔버 (스테이지 검증기)

파싱된 스테이지를 받아 다음을 판정하는 함수 모음:

- `bfsDist(cells, from, to, avoidTraps)` — 벽(및 옵션에 따라 함정)을 막힌 칸으로 보고 최단 거리 계산.
- `validateStage(stage)` 반환값:
  - `reachable`: 모든 재료가 시작점에서 도달 가능한가
  - `perfectPath`: 함정을 밟지 않고 주문 순서대로 먹는 경로가 존재하는가 (별 3개 보장 — 기존 설계 원칙의 자동화)
  - `optimalMoves`: 순서대로 먹는 최단 경로 총 칸 수 → `optimalTime = optimalMoves / MOVES_PER_SEC`
  - `timeOk`: `optimalTime`이 `timeLimit`의 일정 비율(기본 70%) 이하인가
- 개발 시 콘솔에서 기존 5스테이지 전부 검증 통과 확인. 단계 3의 랜덤 맵 검증에 그대로 재사용.

### 1-3. 모드 추상화

- `mode: 'story' | 'endless'` 상태와 `currentStageData`(현재 판의 원본 스테이지 데이터) 도입.
- `STAGES[currentStageIndex]`를 직접 참조하는 곳(재시작 R, 결과 팝업의 다시 하기/다음 스테이지, 확인 팝업 등)을 전부 `currentStageData` 기준으로 변경.
- "다음 판" 로직을 모드별로 분기: story는 `STAGES[index + 1]`, endless는 새 맵 생성 (단계 3에서 구현).
- 이 단계에서는 story 모드만 존재하므로 동작 변화 없음 — 순수 리팩터.

## 단계 2 — 스테이지 6~10

### 2-1. 선택 화면 5×2 그리드

- 스테이지 카드를 한 줄(가로 나열)에서 **5열 × 2행 그리드**로 변경 (카드 140px × 5 + 간격 = 780px, 캔버스 960px 안에 수납).
- 키보드: ←→ 좌우 이동에 ↑↓ 행 이동 추가.

### 2-2. 스테이지 데이터 설계

난이도 축: 맵 크기 / 레시피 길이 / 함정 밀도 / 제한 시간. 기존 1~5는 전부 9×9·20초였으므로 6~10에서 새 축을 연다.

| 스테이지 | 컨셉 | 맵 | 레시피 | 특징 |
|---|---|---|---|---|
| 6 | 대형 매장 | 11×11 | 5 | 넓은 맵 가로지르기, 재료 분산 |
| 7 | 숨바꼭질 | 11×11 | 6 | 재료를 맵 전역에 흩뿌림 |
| 8 | 쓰레기 대란 | 11×11 | 6 | 함정 고밀도, 안전 경로가 좁음 |
| 9 | 비밀 통로 | 11×11 | 7 | 숨은 길목의 미로, 경로 계획 |
| 10 | 라스트 오더 | 13×13 | 8 (전 재료) | 최종 스테이지, 시간 빠듯 |

- 제한 시간은 솔버의 `optimalTime`을 기준으로 역산해 설정 (초반 여유 배수 크게, 10은 빠듯하게).
- 모든 신규 스테이지는 `validateStage` 통과 필수.

## 단계 3 — 무한 모드 (절차 생성)

### 3-1. 시드 RNG

- `mulberry32` 등 시드 기반 PRNG 추가. 라운드마다 시드를 정해 보관.
- **R 재시작 = 같은 시드로 같은 맵 재도전**, 클리어 후 다음 라운드 = 새 시드. (재시작마다 맵이 바뀌면 불공평)
- `parseStage`의 함정 이모지 랜덤도 시드 RNG로 통일.

### 3-2. 맵 생성기 `generateStage(round, seed)`

생성 파이프라인:

1. **미로 생성**: 전부 벽인 홀수 크기 맵에서 recursive backtracker로 통로를 판다.
2. **우회로 추가**: 내부 벽 일부를 확률적으로 제거 (loop factor). 외길 미로는 퍼즐이 아니라 노가다가 되므로 복수 경로 필수.
3. **배치**: 바닥 칸에 S → 레시피 재료(종류별 1개) → 함정 순서로 무작위 배치. S와 첫 재료 사이 최소 거리 등 가벼운 제약.
4. **검증**: `validateStage` 통과 못 하면 시드를 파생시켜 재생성 (9×9 기준 생성+검증 1ms 미만, 수십 회 재시도 무부담).
5. **제한 시간 산출**: `timeLimit = ceil(optimalTime × margin(round))` — 라운드가 오를수록 margin 축소.

### 3-3. 난이도 곡선 (데이터 주도 — 상단 상수로)

- 재료 수: `min(2 + floor(round / 2), 8)`
- 함정 수: 라운드에 비례해 증가 (상한 있음)
- 맵 크기: 라운드 1~5는 9×9, 6~14는 11×11, 15+는 13×13 (`ENDLESS.size(r)` 확정값)
- 시간 여유 배수: 2.2에서 시작해 라운드당 감소, 하한 1.4 (조정 대상)

### 3-4. 게임 흐름·기록

- 진입: 타이틀 화면에 "무한 모드" 버튼 추가.
- 클리어 팝업: "다음 주문 (Enter)" → 다음 라운드 생성. 별점 표시는 유지하되 저장하지 않음.
- 실패 = 런 종료: 도달 라운드 표시, `localStorage`에 별도 키로 `{ bestRound }` 저장·표시.
- 플레이 화면 상단 표기: "Round N" (스테이지 이름 대신).

## 단계 4 — 다국어 지원 (한국어/English)

캔버스는 매 프레임 상태로부터 다시 그려지므로, 언어를 바꾸는 순간 새로고침 없이 즉시 반영된다. 단 모바일 안내와 문서 메타데이터는 DOM이므로, 언어 변경 시 이를 함께 갱신하는 `applyLanguage()`가 필요하다. 페이지 리로드는 필요 없다.

### 4-1. 상태·저장

- `LANG` 상태 변수(`'ko' | 'en'`), `burger-game-lang` localStorage 키.
- 저장된 선택이 없으면 `navigator.language`로 최초 진입 언어를 결정(`ko`로 시작하면 한국어, 그 외 영어). 사용자가 토글하면 즉시 `LANG`을 바꾸고 저장 — 이후 방문에서도 유지.
- localStorage 접근은 이미 있는 `storageGet`/`storageSet` 래퍼를 그대로 쓴다(비공개 모드·사이트 데이터 차단 환경에서 접근 자체가 예외를 던지는 것을 흡수한다). 저장값은 반드시 `'ko' | 'en'`인지 검증하고, 손상되었거나 없으면 브라우저 감지 결과로 fallback한다.

### 4-2. 문자열 사전화

- `STRINGS = { ko: {...}, en: {...} }` 딕셔너리 + `t(key, ...args)` 헬퍼(치환 필요한 곳은 `{n}` 같은 자리표시자 지원)를 추가한다. 모든 번역 데이터는 이 구조로 통일해 `t('stageNames.1')`처럼 조회하며, 언어 쌍을 별도 최상위 구조로 섞지 않는다.
- 현재 하드코딩된 한글 리터럴은 약 58줄 — 전체 완전 번역 방침에 따라 다음을 전부 사전에 옮긴다:
  - 타이틀·선택·플레이 화면의 버튼·안내 문구, HUD 라벨("주문서"/"내 버거"/"라운드 N" 등)
  - 클리어·실패·확인 팝업, 온보딩 팝업의 규칙 설명 5줄×2종
  - **스테이지 이름 10개**(예: "쓰레기 대란", "라스트 오더")는 `STAGES` 데이터에서 분리해 `STRINGS[LANG].stageNames[id]`로 관리. `STAGES`의 `grid`/`recipe`/`timeLimit`은 언어 무관이라 그대로 둔다.
  - **실패 랜덤 코멘트**(`FAIL_COMMENTS_TRAP`/`FAIL_COMMENTS_TIMEOUT`)도 `{ ko, en }` 쌍 배열로 바꾸고, 뽑을 때 현재 `LANG`에 맞는 쪽을 사용.
  - 재료 이름(`INGREDIENTS[code].name`, 예: `patty`)은 순서 판정용 내부 식별자이고 화면엔 이모지만 노출되므로 번역 대상 아님(변경 없음).
- `applyLanguage()`는 모바일 안내의 제목·본문·버튼, `document.documentElement.lang`, `document.title`, meta description도 즉시 갱신한다. Canvas가 아닌 이 요소들을 누락하지 않는다.

### 4-3. 토글 UI

- 우상단에 작은 언어 토글(예: "한국어 / EN" 필 버튼)을 상시 배치하되, 실제 플레이 중(확인/완료/실패 오버레이가 없는 순수 플레이 상태)에는 숨겨 실수 클릭을 막는다 — 커서를 플레이 중에만 숨기는 기존 `updateCursor` 로직과 같은 패턴.
- 토글은 타이틀·선택 화면, 그리고 플레이 화면 위의 각종 팝업(확인/클리어/실패/온보딩)에서는 노출.

### 4-4. 레이아웃 리스크 대응

- 버튼·카드 크기가 대부분 한국어 문구 기준으로 고정 폭(예: 220px, 140px)이다. 영어 문구가 이보다 길어지는 지점(예: "무한 모드 (Space)" → "Endless Mode (Space)")이 있는지 각 버튼을 실측(`ctx.measureText`)해 확인하고, 넘치는 곳만 폭을 여유 있게 넓힌다 — 레이아웃 시스템을 새로 만들기보다 기존 "화면별 명시적 하드코딩" 원칙을 유지한 채 필요한 곳만 수정.
- 온보딩 카드처럼 문장형 텍스트가 있는 곳은 언어별로 줄바꿈 위치가 달라질 수 있어 두 언어 모두 실제 렌더로 확인.

### 4-5. 검증

- 현재 저장소에 재사용할 Node 하니스는 없으므로, (a) 두 언어에서 실제 브라우저 수동 스모크 테스트를 수행하거나 (b) 의존성 없는 최소 하니스를 새로 추가한다. 두 언어(`ko`/`en`) 각각에서 전체 화면·모든 팝업·10스테이지·무한 모드 렌더 및 모바일 안내 DOM을 예외 없이 확인한다.
- 솔버(`validateStage`)는 언어와 무관하므로 영향 없음 — 기존 검증 그대로 유지.

## 단계 5 — 배포·수익화 (다른 단계와 병행 가능)

심사 대기가 수 주 걸리므로 도메인·신청은 일찍 시작하는 것이 유리.

1. **도메인 구입** (잠정 확정): Cloudflare Registrar / Namecheap / 가비아 등, 연 1.5~2만원. 이는 AdSense의 기술적 필수조건이 아니라 브랜딩·장기 운영 선택이다.
2. **GitHub Pages 커스텀 도메인 연결**: 저장소 설정 + DNS(CNAME), HTTPS 자동 발급. 호스팅 이전 없음. 필요 시 Pages 설정에 맞춰 `CNAME` 파일도 관리한다.
3. **페이지 구조와 콘텐츠 보강**: 현재 `body`가 캔버스를 중앙 정렬하는 flex 컨테이너이므로, 캔버스 아래 본문을 바로 추가하지 않는다. `main`/wrapper를 세로 문서 흐름으로 바꾸고, 게임 소개·조작법·별점 규칙·개인정보처리방침 링크를 실제 HTML 텍스트로 제공한다. 모바일 표시 방침(위 정의)도 이 구조에서 검증한다.
4. **`privacy.html`**: 개인정보처리방침 페이지를 작성한다. localStorage 사용, Google을 포함한 제3자 광고 공급자·광고 쿠키, 맞춤 광고 및 옵트아웃 관련 안내를 실제 운영 설정에 맞춰 포함한다.
5. **AdSense 사이트 준비·신청**: 사이트를 등록하고 소유권 확인 및 정책 검토가 완료되어 `Ready` 상태가 된 뒤에만 광고를 게재한다. `ads.txt`도 배포해 계정 외 판매를 방지한다.
6. **승인 후 광고 배치**: 광고 유닛은 캔버스 밖(상/하단)의 독립된 본문 영역에 배치한다.
   - 광고 스크립트는 명시적인 배포 호스트 allowlist에만 로드한다. 로컬 더블클릭·localhost·미리보기 환경에서는 광고 없이 동작해야 한다.
   - EEA·UK·스위스 대상 동의는 Google 인증 CMP(AdSense Privacy & messaging 포함)로 구성한다. CMP 사용 여부와 별개로 개인정보처리방침·동의 문구의 법적 적합성은 운영자가 검토한다.
7. **기대치**: 취미 트래픽 기준 수익은 사실상 0에 수렴. 이 단계의 목적은 도메인 → 배포 → 심사 → 광고 부착 전 과정의 학습.

## 미결 사항

- 도메인 이름·등록 업체 선정 (사용자 결정)
- 무한 모드 난이도 상수의 실제 값 (플레이 테스트로 조정)
- AdSense 거절 시 대응: 콘텐츠 보강 재신청 vs 게임 포털(itch.io 등) 병행
- 영어 번역 문구의 원어민 감수 여부 (우선 직역으로 시작, 필요 시 다듬기)
- 언어 토글의 정확한 위치·크기 (다른 UI 요소와 겹치지 않는지 실제 화면에서 확인 필요)

## (선택) 향후 리팩토링 및 구조 개선 제안

현재 단일 `index.html` 파일로 모든 기능이 완벽하게 동작하고 있으나, 파일 크기(2,300+ 줄)와 하드코딩된 UI 렌더링 방식의 한계로 인해 추후 대규모 기능 추가 시 유지보수가 어려워질 수 있습니다. 향후 여력이 될 때 아래의 개선 작업들을 고려해볼 수 있습니다.

1. **역할별 파일 분리 (모듈화)**
   - `constants.js`: 상수, 맵 데이터(`STAGES`), 레시피, 에셋 데이터 분리
   - `engine.js`: 이동, 충돌, 시간, 맵 생성 알고리즘 분리
   - `ui.js`: Canvas 렌더링, 팝업, UI 토큰 로직 분리
   - 이점: 코드 파악과 디버깅이 훨씬 쾌적해집니다.
2. **Canvas UI 레이아웃 시스템 도입**
   - 현재 팝업 UI는 픽셀 좌표(`x`, `y`)를 직접 계산하여 하드코딩으로 렌더링하고 있습니다.
   - 웹의 Flexbox처럼 `padding`, `margin`, `gap`, `align-items` 등의 개념을 처리해주는 **경량형 레이아웃 함수**를 도입하면 텍스트 길이나 화면 크기 변화에 유연하게 대응할 수 있고 UI 버그를 획기적으로 줄일 수 있습니다.
3. **게임 루프 상태 머신(State Machine) 도입**
   - 화면 전환(`screen = 'play' | 'select'`)과 팝업 렌더링 로직이 다소 엉켜 있습니다. 명시적인 상태 머신 패턴을 도입하여 상태 전환 관리를 견고하게 만들 수 있습니다.

### 검토 의견 (2026-07) — 셋 다 지금은 보류, 트리거 조건이 오면 재검토

1. **파일 분리 → 사실상 반대.** CLAUDE.md의 핵심 원칙(단일 파일·무빌드·더블클릭 실행)과 정면 충돌한다.
   ES 모듈(`import`/`export`)은 `file://`에서 CORS로 막혀 항상 로컬 서버가 필요해지고, 일반 `<script>` 여러 개로
   쪼개는 방법은 더블클릭은 되지만 전역 스코프를 그냥 공유하는 것이라 실질적 모듈화 이점이 거의 없다.
   코드 탐색이 불편한 게 진짜 문제라면 에디터 폴딩/섹션 점프로 충분. **트리거**: 코드가 4,000줄대(현재의 2배)로
   커지거나, 더블클릭 실행을 포기하기로 결정할 때.
2. **레이아웃 시스템 → 반대. 이미 CLAUDE.md에 반대 결론이 문서화돼 있음.** "UI 그리기 규칙"의 "명시적 하드코딩:
   특정 화면에 종속된 레이아웃 배치는 캔버스 환경의 직관성과 유지보수 편의를 위해 의도적으로 하드코딩한다"와
   정확히 배치되는 제안이다. 범용 레이아웃 엔진은 지금 팝업 개수(4~5개)에 비해 도입·검증 비용이 크고, "세 줄의
   유사한 코드가 섣부른 추상화보다 낫다"는 코딩 컨벤션에도 안 맞는다. 다만 v3 다국어(단계 4-4)에서 나온 좁은
   문제 — 영어 문구가 고정폭 버튼을 넘칠 수 있음 — 는 실존 리스크이므로, 범용 시스템 대신 `ctx.measureText`
   기반 자동폭 버튼 헬퍼 정도의 좁은 대안만 필요 시 4-4에서 추가한다. **트리거**: 팝업 개수가 지금의 2배 이상으로
   늘어 유지보수 고통이 실제로 느껴질 때.
3. **상태 머신 → 판단 보류.** `screen`/`mode`/`completed`/`failed`/`confirming`/`onboarding` 플래그가 흩어져
   있지만, 입력 핸들러의 우선순위 if-체인이 사실상 이미 상태 머신 역할을 하고 있고 실제 버그로 이어진 적은
   없다(매 기능 추가 때 렌더 스모크 테스트로 확인). "지금 팝업이 떠서 입력을 막아야 하는가" 판단이 커서 숨김
   로직, 그리고 v3 언어 토글 숨김 로직에서 벌써 두 번째로 반복되고 있는 점만 기억해둘 것. **트리거**: 이 판단
   로직이 세 번째로 중복될 때 `isBlockingOverlay()` 같은 작은 헬퍼로 통합.
