# CLAUDE.md

Burger Game 개발을 위한 가이드. 상세 기획은 [prd.md](prd.md) 참조.

## 프로젝트 개요

빈 번 두 장짜리 햄버거 캐릭터가 격자 맵을 돌아다니며 주문서 순서대로 재료를 먹어 제한 시간 안에 버거를 완성하는 경로 퍼즐 게임. 학습용 사이드 프로젝트.

두 가지 모드가 있다:
- **스테이지 모드**: 손으로 설계한 10개 스테이지(9×9 → 13×13). 클리어로 다음 스테이지 잠금 해제, 스테이지별 최고 별점 저장.
- **무한 모드**: 라운드마다 절차 생성한 랜덤 맵을 무한히 플레이. 목숨 3개(실패 시 차감, 3별 클리어로 회복), 소진 시 라운드1부터. 최고 라운드·평균 별점 기록. 난이도는 15라운드에서 상한에 도달해 지구력전이 된다.

현재 진행 상황과 향후 로드맵(배포·수익화 등)은 [plan.md](plan.md) 참조. 스테이지 확장·무한 모드는 완료 상태.

## 핵심 원칙

- **단일 파일 유지**: 모든 코드(HTML/CSS/JS/SVG 문자열)를 `index.html` 하나에 담는다. 라이브러리·빌드 도구·번들러 없이 브라우저에서 더블클릭으로 바로 실행된다.
- **의존성 제로**: npm, CDN, 외부 스크립트 금지. 순수 브라우저 API(Canvas, localStorage)만 사용.
  - 예외: 웹폰트(Pretendard)는 CDN `<link>`로 로드한다. 오프라인에서는 시스템 sans-serif로 자연스럽게 대체된다.
- **데이터 주도**: 스테이지는 파일 상단 상수 배열의 JSON으로 정의한다. 레벨 추가 = 배열에 항목 추가만으로 가능해야 한다. 게임 로직에 스테이지별 하드코딩 금지. 무한 모드 난이도 곡선도 `ENDLESS` 상수 하나로 조정한다.
- **풀이 가능성 보장**: 모든 스테이지와 랜덤 생성 맵은 솔버(`validateStage`, BFS 기반)로 "함정·순서를 지켜 별 3개를 받는 경로가 실제로 존재함"을 검증한다. 새 스테이지를 추가하면 시작 시 `verifyStages()`가 콘솔로 경고한다.
- **데이터 형식 검증**: 솔버는 "풀 수 있는가"만 보므로 격자 오타를 잡지 못한다. 행 길이가 어긋나거나 모르는 문자가 섞이면 `parseStage`에서 조용히 빈 바닥이 되어, 벽이 사라진 맵도 솔버를 그대로 통과한다. `checkStageData()`가 행 길이·`S` 개수·미지의 문자·재료 배치(종류별 1개, 레시피에 없는 재료 금지)를 점검해 `verifyStages()`에서 함께 경고한다. 새 스테이지를 추가한 뒤에는 콘솔을 확인한다.

## 실행 방법

빌드 없음. `index.html`을 브라우저로 열면 된다.

```
open index.html
```

동작 확인이 필요하면 브라우저에서 실제로 플레이해 검증한다 (테스트 프레임워크 없음).

## 아키텍처 방향

단일 파일 안에서 관심사를 함수/모듈 단위로 구분한다:

- **데이터**: `STAGES` 상수 배열, `ENDLESS` 난이도 곡선, 재료·함정·랜덤 코멘트 정의, SVG 문자열 상수
- **솔버**: `validateStage`/`bfsDist` — 스테이지 검증. 무한 모드 생성기도 이걸 재사용
- **무한 생성기**: `generateStage`/`buildEndlessMap` — 미로 생성 + 재료 분산 + 지름길 함정 + 솔버 검증 (문자 격자용 BFS `bfsPathChar`는 검증용 `bfsDist`와 별개)
- **상태**: 현재 화면, `mode`(story/endless), 캐릭터 위치, 먹은 스택, 남은 시간, 목숨, 진행 저장(localStorage)
  - localStorage는 반드시 `storageGet`/`storageSet` 래퍼로만 접근한다. 비공개 모드·사이트 데이터 차단
    환경에서는 `localStorage` 접근 자체가 예외를 던지는데, 저장 경로가 게임 루프(`update`) 안에 있어
    직접 호출하면 판을 깨는 순간 게임이 멈춘다. 저장 실패는 조용히 무시하는 것이 정책이다.
- **게임 루프**: `requestAnimationFrame` 기반 update/render 분리
- **입력**: 키보드 핸들러 (방향키·WASD 이동, R 재시작[무한 모드는 비활성], ESC 나가기) + 캔버스 클릭(hitAreas)
- **렌더**: Canvas에 맵·캐릭터·주문서·버거 스택·시간 바·팝업 그리기. 화면별 함수(`renderTitle`/`renderSelect`/`renderPlay` 등)로 분리
- **화면 전환**: 타이틀 → 선택 → 플레이 → 결과, 그리고 온보딩 팝업(모드별 첫 진입 1회)
  - **오버레이 판정 단일화**: 결과 팝업은 연출(번 낙하 700ms / 하트 깨짐 650ms)이 끝난 뒤 나타나므로,
    "지금 팝업이 실제로 보이는가"를 `clearPopupShown`/`failPopupShown`/`isBlockingOverlay`로만 판단한다.
    렌더·키 입력·커서가 이 판정을 공유해야 팝업이 보이기 전에 키가 먹어 결과 화면을 건너뛰는 일이 없다.
    새로 "플레이 중에만" 동작하는 요소(예: v3 언어 토글)를 넣을 때도 조건을 새로 쓰지 말고 이걸 쓴다.

**UI 그리기 규칙 (디자인 토큰 및 하드코딩 현황)**:
- **재사용 토큰**: 색상은 `COLORS`, 폰트 크기는 `FONT` 객체(hero, h1, body 등), 폰트 패밀리는 `uiFont(size, weight)`로 중앙 관리한다. 공통 수치는 `UI` 객체(radius, btnW, btnH, gap)를 사용한다.
- **재사용 컴포넌트**: `drawCard`(팝업), `drawButton`(버튼), `drawModeButton`, `drawTextLink`, `drawPanel`, `drawPanelHeader`, `drawHearts`, `drawLockIcon` 등 반복되는 요소는 전용 헬퍼 함수를 호출한다. UI 수정 시 이 헬퍼/토큰을 우선 사용한다.
- **명시적 하드코딩**: 특정 화면에 종속된 레이아웃 배치는 캔버스 환경의 직관성과 유지보수 편의를 위해 의도적으로 하드코딩한다.
  - **팝업 크기**: 상태별 고정 크기 사용. 폭×높이는 각 렌더 함수 안에서 다음과 같이 결정된다.
    - 온보딩(`renderOnboardingPopup`): 무한 540x400 / 스토리 440x340
    - 클리어(`renderClearPopup`): 폭 360 고정, 높이는 무한 280 / 스토리는 다음 판 있으면 360, 마지막 판이면 320
    - 실패(`renderFailPopup`): 340x300 고정 (목숨 잔여 여부와 무관하게 동일 크기)
    - 확인(`renderConfirmPopup`): 340x200 
  - **내부 요소 및 좌표**: 팝업 내 텍스트 위치(`cardCy ± n`), 선택 화면 그리드 시작점, 인게임 패널 크기 및 여백 등은 해당 렌더 함수 내부에서 고정 값으로 처리한다.

## 좌표·데이터 규칙 (prd.md 기준)

- grid는 문자열 배열, 배열 크기가 곧 맵 크기 (9×9 ~ 13×13, 셀 크기는 `stage.cell`로 자동 계산)
- 문자 매핑: `#` 벽 / `.` 빈칸 / `S` 시작 / `X` 함정 / 재료 코드 `P C L T O B E K` (재료 8종)
- 재료는 종류별 1개만 배치, 여분의 정상 재료 없음
- 채점: 별1(시간 내 완성) / 별2(함정 0개) / 별3(순서 정확), 완성하면 별1은 항상 획득

## 개발 진행 순서

prd.md의 초기 5단계(맵·이동 → 재료·스택 → 주문서·타이머·별점 → 화면 흐름 → SVG·5스테이지)는 **완료**됐고, 이후 확장(스테이지 10개, 무한 모드, 목숨·온보딩 등)도 반영돼 있다. 현재 남은 로드맵(배포·수익화 등)과 지금까지의 결정 사항은 [plan.md](plan.md)에 정리돼 있으니 새 작업 전에 참고한다.

각 변경은 실제로 플레이 가능한 상태로 마무리하고, 스테이지/맵 관련 변경은 솔버 검증을 통과해야 한다.

## 코딩 컨벤션

- 주석·식별자는 주변 코드 스타일에 맞춘다. 기획 용어(재료 코드, 채점 규칙 등)는 prd.md와 일치시킨다.
- 매직 넘버(셀 크기, 캔버스 크기, 이동 속도 등)는 상단 상수로 모은다.
- 새 재료·함정·코멘트·스테이지 추가가 데이터 수정만으로 되도록 로직과 데이터를 분리한다.
