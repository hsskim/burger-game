# [PRD] Burger Game

## 개요

- **한 줄 요약**: 빈 번 두 장짜리 귀여운 햄버거 캐릭터가 격자 맵을 돌아다니며, 주문서 순서대로 재료를 모아 제한 시간 안에 햄버거를 완성하는 길찾기 퍼즐 게임
- **플랫폼**: 웹 브라우저, 데스크톱 우선, 키보드 조작
- **목적**: 학습 겸 재미로 만드는 사이드 프로젝트
- **검증 목표**: "경로로 함정을 피하는 플레이가 실제로 재미있는가"를 5스테이지 프로토타입으로 확인

## 기술 스택

- 순수 HTML/JS + Canvas, `index.html` 단일 파일 (라이브러리·빌드 없음, 더블클릭 실행)
- 진행 저장: localStorage (스테이지별 획득 별, 잠금 해제 상태)

## 핵심 규칙

- 격자 단위 이동, 재료는 맵에 고정 배치 (순수 길찾기 퍼즐)
- 캐릭터는 빈 번 두 장 상태로 시작, 재료 셀에 진입하면 즉시 섭취 (취소 불가)
- 먹은 순서 그대로 버거에 쌓이며, 함정 오브젝트도 그대로 쌓여 웃긴 버거가 됨
- 주문서 재료를 전부 먹으면 상단 번이 덮이며 자동 완성 → 순서가 틀려도 클리어
- 시간 제한은 실패 조건: 타임오버 시 미완성 버거를 보여주고 R로 즉시 재시작
- 맵에는 주문서 재료(종류별 1개)와 함정 오브젝트만 배치 — 여분의 정상 재료는 두지 않음
- 벽 셀은 통과 불가

## 채점 (별점 3개)

- **별 1**: 시간 내에 주문서 재료를 전부 먹어 완성함 (클리어 시 항상 획득)
- **별 2**: 함정 오브젝트를 하나도 먹지 않음
- **별 3**: 주문서 재료를 주문서 순서대로 먹음 (함정은 순서 판정에서 제외)
- 클리어하면 최소 별 1개는 보장 → 별 2·3 도전으로 재도전 루프 형성

## 화면 흐름

1. **타이틀**: 게임 제목 + 시작 버튼
2. **스테이지 선택**: 1~5번, 획득 별 표시, 순차 잠금 해제
3. **플레이**: 아래 레이아웃 참조
4. **결과**: 클리어(별점 + 완성 버거 + 다음 스테이지 버튼) / 실패(미완성 버거 + 랜덤 코멘트)
5. **결과 화면 공통 키**: `R` 다시 하기, `ESC` 스테이지 선택으로

## 플레이 화면 레이아웃 *(수치는 제안값)*

- 캔버스: 960×640 고정
- 중앙: 9×9 격자 맵 (셀 48px)
- 상단: 남은 시간 바
- 좌측: 주문서 — 레시피 순서 표시, 다음에 먹을 재료 하이라이트
- 우측: 현재까지 쌓인 버거 스택 (아래→위)

## 조작

- **방향키/WASD**: 한 번 누르면 한 칸, 꾹 누르면 연속 이동 (초당 최대 6칸)
- **R**: 즉시 재시작
- **ESC**: 스테이지 선택으로

## 비주얼

### 캐릭터 — 인라인 SVG

- 빈 번 두 장 사이로 큰 눈이 보이는 귀여운 디자인 (짧은 팔다리 옵션)
- SVG 문자열을 JS에 포함 → Image 객체로 변환해 Canvas에 렌더 (단일 파일 유지)
- 표정 3종: 기본 / 냠냠(재료 섭취 순간) / 시무룩(실패 화면)

### 맵 아이템 — 이모지

- **재료**: 🥩 패티, 🧀 치즈, 🥬 양상추, 🍅 토마토, 🧅 양파, 🥓 베이컨, 🍳 달걀, 🥒 피클
- **함정**: 🧦 양말, 🧼 비누, 👟 신발, 🧽 수세미, 🎱 당구공
- **장애물**: 🧱 벽
- 맵 위 아이템은 이모지 유지 (작은 셀에서 가독성이 좋고 제작 부담 없음)

### 버거 스택 — 레이어형 SVG

- 우측 버거 현황 패널은 이모지 아이콘이 아니라 실제 재료처럼 생긴 레이어형 SVG를 겹쳐 쌓아 표현
- 재료 8종 + 함정 5종 각각 납작한 레이어 1개씩 정의 (패티는 두툼한 갈색 타원, 양상추는 물결 프릴, 치즈는 흘러내리는 사각형 등)
- 위쪽 번에는 귀여운 눈이 달려 있음 → 완성 순간 눈 달린 번이 덮이며 캐릭터와 연결되는 인상
- 캐릭터와 동일하게 SVG 문자열을 JS에 포함해 Canvas에 렌더 (단일 파일 유지)
- 결과 화면의 완성/미완성 버거도 같은 레이어 SVG 재사용

## 스테이지 데이터 구조 (JSON)

```json
{
  "id": 1,
  "name": "첫 주문",
  "timeLimit": 60,
  "recipe": ["patty", "cheese"],
  "grid": [
    "#########",
    "#S..P...#",
    "#..###..#",
    "#..C..X.#",
    "#########"
  ]
}
```

- **문자 매핑**: `#` 벽, `.` 빈칸, `S` 시작 위치, `X` 함정(이모지 랜덤 표시)
- **재료 코드**: `P` 패티, `C` 치즈, `L` 양상추, `T` 토마토, `O` 양파, `B` 베이컨, `E` 달걀, `K` 피클
- grid 문자열 배열의 크기가 곧 맵 크기 → 9×9는 기본값이며 스테이지마다 가변 가능
- 스테이지 5개를 파일 상단 상수 배열로 정의 → 레벨 추가 = JSON 항목 추가만으로 가능

## 5스테이지 구성안

1. 재료 2개, 함정 0개 — 조작·규칙 학습
2. 재료 3개, 함정 2개 — 회피 개념 도입
3. 재료 4개, 함정 4개, 벽 등장 — 경로 계획 필요
4. 재료 5개, 함정 6개, 좁은 통로 — 순서 최적화
5. 재료 7개, 함정 8개, 시간 빠듯 — 종합 난이도

## 연출

- **필수**: 재료 섭취 시 스택에 통통 튀며 쌓이는 애니메이션, 캐릭터 냠냠 표정 전환
- **필수**: 실패 화면 미완성 버거 + 랜덤 코멘트 (JS 배열에 10개 내외 정의, 예: "이건... 판매할 수 없어요", "양말버거 나왔습니다")

## 바이브코딩 진행 순서 (Claude Code 단계별 프롬프트)

1. 격자 맵 렌더링 + 캐릭터 이동 (캐릭터는 임시로 🍔 사용)
2. 재료 섭취 + 버거 스택 표시 (임시 이모지 스택)
3. 주문서·타이머·별점 로직 + localStorage 저장
4. 화면 흐름 (타이틀/선택/결과)
5. SVG 제작·교체 (캐릭터 + 재료·함정 레이어 + 눈 달린 위쪽 번) + 5스테이지 데이터 + 연출 다듬기
