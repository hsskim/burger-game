# Burger Game UI 구조 분석

현재 `index.html` 내부에 구현된 UI 요소들 중 **재사용(모듈화)된 부분**과, 특정 화면에 종속되어 **명시적으로 하드코딩된 부분**을 정리한 문서입니다.

## 1. 재사용 중인 UI 요소 (공통 컴포넌트 & 토큰)

코드 최상단과 헬퍼 함수 영역에 정의되어 게임 전반에 걸쳐 반복적으로 사용되는 요소들입니다.

### 🎨 디자인 토큰 (상수 객체)
모든 렌더링 함수가 공유하는 시각적 규칙입니다.
*   **`COLORS`**: 게임 전체의 색상 팔레트 (`bg`, `panel`, `text`, `accent`, `star` 등)
*   **`FONT`**: 텍스트의 위계별 폰트 사이즈 (`hero: 56`, `h1: 38`, `body: 20`, `caption: 14` 등)
*   **`UI`**: 공통 레이아웃 수치 (`radius: 20`, `btnW: 200`, `btnH: 48`, `gap: 24` 등)

### 🧩 공통 UI 렌더링 함수 (재사용 컴포넌트)
*   `uiFont(size, weight)`: "Pretendard" 폰트 패밀리를 일관되게 적용해 주는 텍스트 래퍼 함수.
*   `drawCard(x, y, w, h, r)`: 모서리가 둥글고 그림자가 있는 팝업/패널의 기본 배경을 그리는 함수.
*   `drawButton(x, y, w, h, text, action, fontSize)`: 마우스 호버 효과(스케일업, 색상 변경)와 클릭 이벤트를 포함한 공통 버튼 렌더링 함수.
*   `drawModeButton(...)`: 타이틀 화면의 '스토리 / 무한' 모드 선택 버튼 전용 래퍼 (아이콘과 설명 포함).
*   `drawTextLink(x, y, text, action)`: 팝업창 하단의 텍스트형 링크 버튼(예: "타이틀로 나가기 (ESC)") 렌더링.
*   `drawPanel(x, y, w, h)` / `drawPanelHeader(...)`: 게임 플레이 화면 좌우측의 반투명 HUD 패널과 제목을 그리는 함수.
*   `drawHearts(x, y, lives, align)`: 무한 모드 실패 창이나 플레이 HUD에서 하트 개수를 그려주는 함수.

---

## 2. 명시적으로 하드코딩된 UI 요소

범용적으로 쓰이지 않고, **특정 화면(문맥)의 레이아웃 배치를 위해 직접 픽셀 값이나 분기 처리가 고정된 부분**들입니다.

### 📐 팝업창 전체 크기 (가로/세로)
최근 리팩토링을 통해 동적 계산을 제거하고 상태별로 크기를 고정했습니다.
*   **온보딩 팝업 (`renderOnboardingPopup`)**:
    *   무한 모드: `540 x 400`
    *   스토리 모드: `440 x 340`
*   **클리어 팝업 (`renderClearPopup`)**:
    *   스테이지 클리어 (다음 판 있음): `360 x 360`
    *   스테이지 올 클리어 (다음 판 없음): `360 x 320`
    *   무한 모드 클리어: `360 x 324` (하단 텍스트 링크가 1줄 적어 높이 축소)
*   **실패 팝업 (`renderFailPopup`)**:
    *   기본 실패 (스테이지/무한 목숨 남음): `340 x 300`
    *   무한 모드 완전 실패 (목숨 0): `340 x 264` (버튼 감소로 높이 축소)
*   **확인 팝업 (`renderConfirmPopup`)**: `360 x 200` 고정

### 📍 내부 요소 배치 (좌표 및 여백)
*   **팝업 내부의 텍스트 위치**: `cardCy - 80` (제목), `cardCy + 18` (최고 기록) 등 팝업 중심점(`cy`)을 기준으로 더하고 빼는 방식의 Y좌표 하드코딩.
*   **스테이지 선택 그리드 (`renderSelect`)**:
    *   카드 크기: `150 x 160`, 카드 간 여백: `24px`
    *   그리드 시작점: Y축 `160px` 고정.
*   **타이틀 화면 (`renderTitle`)**:
    *   로고 위치: Y축 `120px`
    *   모드 버튼 Y축 위치: `300px`, 버튼 간격 `32px`.
*   **게임 인게임 HUD**: 
    *   좌우 패널 너비(`SIDE_W = 200`)와 위치, 상단 타이머 바의 크기(`240 x 16`) 및 위치(`Y: 20`).

---

## 💡 요약 및 향후 개선 방향
- **색상, 폰트, 공통 UI 요소(버튼/카드 모양)**는 완벽하게 토큰화 및 모듈화되어 재사용되고 있습니다.
- **각 화면별 레이아웃 배치(좌표와 크기)**는 유지보수의 직관성을 위해 캔버스 환경에 맞게 하드코딩되어 있습니다. 
- 만약 추후 "게임 내 다양한 크기의 팝업 10종류"가 추가로 필요해진다면, 하드코딩된 팝업 크기를 `POPUP_SIZES` 같은 토큰으로 한 번 더 빼내는 리팩토링을 고려할 수 있습니다. 하지만 현재 규모에서는 직관적인 하드코딩이 가장 효율적인 상태입니다.
